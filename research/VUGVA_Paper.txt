        VUGVA: A Software-Defined Virtual Unified GPU VRAM
                 Architecture with CPU-Bypass Hybrid Memory
                          for Non-NVLink Multi-GPU Clusters
                                                        Irfan
                                               Independent Research
                                             irfan@furylogic.com

                                                     July 2026


Abstract                                                         demonstrates that VUGVA achieves 85% of enterprise
                                                                 NVLink performance at less than 15% of deployment
Distributed execution of Frontier-class Large Language           cost, while the CPU-bypass extension reduces CPU uti-
Models (LLMs)—such as DeepSeek-V3/R1 (671B pa-                   lization by 84.6% and improves throughput by 54.8%
rameters) and Llama-3-405B—is severely constrained by            compared to traditional CPU-mediated memory offload-
the physical memory boundaries of individual GPUs.               ing.
Enterprise data centers mitigate this through hardware-
level cache-coherent interconnects like NVIDIA NVLink
and NVSwitch, but consumer and budget-constrained                1    Introduction & The Hardware Wall
research labs running commodity hardware face catas-
                                                                 The memory footprint required to host modern flagship
trophic communication bottlenecks over standard PCIe
                                                                 Mixture-of-Experts (MoE) and dense LLM architectures
topologies. Furthermore, when aggregate GPU VRAM is
                                                                 exceeds the VRAM capacity of any single commercial
insufficient to host a model entirely, existing frameworks
                                                                 graphics card. Table 1 illustrates the scale of this chal-
fall back to CPU-mediated memory offloading, which in-
                                                                 lenge for representative Frontier-class models.
troduces severe latency penalties and CPU cache pollu-
tion.
   This paper presents VUGVA (Virtual Unified GPU                Table 1: Memory requirements of Frontier-class LLMs.
VRAM Architecture), a software-defined memory vir-                Model                 Params    FP16 (GB)    4-bit (GB)
tualization layer that abstracts multiple physically dis-         Llama-3-8B                 8B           16          4.5
crete GPU VRAM pools into a single contiguous virtual             Llama-3-70B              70B           140          35
address space. We extend this architecture in VUGVA               Llama-3-405B            405B           810         205
v2 to incorporate system DRAM (RAM) as a unified                  DeepSeek-V3             671B         1,342         340
second-tier memory pool, creating a three-tier hierar-            Gemini Ultra (est.)      1.8T        3,600         900
chy (VRAM→DRAM→SSD) with full CPU bypass—
the CPU never handles data-plane traffic. By inter-                 To run or fine-tune these models, practitioners parti-
cepting CUDA runtime allocation calls, implementing              tion model parameters across multiple GPUs using Ten-
NUMA-aware Peer-to-Peer virtual paging, and employ-              sor Parallelism (TP) or Pipeline Parallelism (PP). In
ing GPUDirect Storage-based DMA engines for kernel-              the absence of high-speed NVLink channels (which of-
bypass data movement, VUGVA transforms commodity                 fer up to 900 GB/s bidirectional inter-GPU bandwidth),
non-NVLink GPU clusters with accompanying system                 multi-GPU systems must route data across the moth-
RAM into a single monolithic high-capacity accelera-             erboard’s PCIe bus via physical PCIe switches (PEX
tor. Our asynchronous Look-Ahead Attention Tracking              chips). This layout introduces severe asymmetric perfor-
engine prefetches upcoming weight matrices over PCIe             mance degradation—the Hardware Wall:
lanes, hiding hardware transport latencies behind active         • Intra-GPU Bandwidth: Local memory access within
Tensor Core compute cycles. Experimental evaluation                 a single GPU runs at native speeds up to 1,008 GB/s

                                                             1
  via the internal GDDR6X bus.                                with 54.8% throughput improvement over CPU-
• Inter-GPU Bandwidth: Remote memory access                   mediated offloading.
  between two discrete GPUs over a physical PCIe
  Gen 4 ×16 link drops to a maximum theoretical
  throughput of 31.5 GB/s, while simultaneously in-
  creasing transactional latency by a factor of 10×–50×. 2 VUGVA Architectural Topography
• CPU-Mediated DRAM→VRAM Bandwidth: When
  frameworks offload to system RAM, the CPU-driven VUGVA operates directly within user space as an in-
  memcpy path achieves only 25–64 GB/s with 15–50 µs termediate orchestration layer positioned between the
  per transfer.                                          deep learning framework (PyTorch, vLLM, SGLang) and
                                                         the native NVIDIA CUDA Driver API (libcuda.so).
1.1 The Extended Memory Wall                             Figure 1 presents the complete system architecture.
Beyond inter-GPU fragmentation, a second boundary                   The system consists of five foundational components:
persists: the VRAM Wall. Even with 8× 48 GB RTX                     1. CUDA Runtime Interceptor:                  Intercepts
4090s yielding 384 GB of aggregate VRAM, models like                   cudaMalloc, cudaMallocManaged, and
DeepSeek-V3 (340 GB in 4-bit) leave no capacity for                    cudaFree commands using dynamic library
KV-cache or intermediate activations. Current frame-                   preloading (LD_PRELOAD).
works respond with:                                                 2. Virtual Memory Table (VMT): A global state ma-
   1. OOM Failure — the application crashes.                           chine mapping virtual addresses exposed to frame-
   2. CPU Offloading (e.g., DeepSpeed ZeRO-Infinity)                   works down to physical, chunked byte offsets dis-
      — tensors route through CPU RAM, incurring                       tributed across GPU VRAM and system DRAM.
      catastrophic bandwidth penalties and consuming                3. Tiered Page Manager (TPM): Manages
      CPU cycles.                                                      the three-tier hierarchy, handling promotion
   The CPU offloading path is fundamentally broken for                 (DRAM→VRAM), demotion (VRAM→DRAM),
real-time inference due to three compounding factors:                  and spilling (DRAM→SSD) based on access
(i) CPU context switching and kernel/user mode tran-                   frequency and recency.
sitions at 15–50 µs per transfer, (ii) L3 cache pollution           4. GPU NUMA Router: A hardware-aware traffic
where tensor data evicts the CPU’s working set, and                    controller that calculates physical distance between
(iii) NUMA node crossing penalties when DRAM re-                       computing Tensor Cores and requested tensor seg-
sides on a different socket than the GPU’s PCIe root com-              ments across the PCIe switch and NUMA topology.
plex.                                                               5. CPU-Bypass DMA Engine: Utilizes GPUDirect
                                                                       Storage and IOMMU P2P mappings to execute data
1.2   Contributions                                                    transfers between DRAM and VRAM without CPU
                                                                       intervention.
This paper makes the following contributions:
 1. VUGVA (v1): A user-space memory virtualization
     layer that unifies discrete non-NVLink GPU VRAM
     pools into a single virtual address space via CUDA         3      Core Software Engineering Mechan-
     runtime interception and NUMA-aware P2P virtual
     paging.
                                                                       ics
 2. VUGVA v2: An extension incorporating system
     DRAM as a unified second tier with three-tier mem-         3.1     Custom CUDA Memory Allocation Inter-
     ory hierarchy (VRAM→DRAM→SSD) and full                             ception
     CPU bypass via GPUDirect Storage DMA engines.
 3. Asynchronous Look-Ahead Attention Tracking:                 When an LLM initialization script launches, PyTorch
     A predictive prefetch engine that hides PCIe trans-        calculates the total tensor weight layout and requests a
     port latencies by overlapping DRAM→VRAM                    massive, contiguous block of VRAM. Instead of trigger-
     transfers with active Tensor Core computation.             ing an Out-Of-Memory error or failing due to hardware
 4. Comprehensive evaluation demonstrating 85% of               limits, the VUGVA interceptor catches the initialization
     enterprise NVLink performance at <15% cost,                request and routes it through the virtual memory engine.

                                                            2
                                                     Deep Learning Framework
                                                     (PyTorch / vLLM / SGLang)
                                                      VUGVA v2 Memory Engine


                                                      VUGVA Unified Mem-
                                                       ory Routing Engine
                                                  Virtual Memory Table | Tiered Page
                                                   Manager | DMA Descriptor Ring

                                                    CPU Bypass Control Plane
                                                      Sets up DMA descriptors,
                                                    never touches data-plane traffic

                                         DMA                           DMA             GDS
            Tier 0                                             Tier 1                                       Tier 2
          GPU VRAM                Promote/Evict            System DRAM                 Spill/Load         NVMe SSD
       Hot: Active Layers                              Warm: Next N Layers                                 Cold: Spill
           1,008 GB/s                                      28–58 GB/s                                       5–7 GB/s


Figure 1: VUGVA v2 Three-Tier Hybrid Memory Architecture. The CPU operates exclusively in the control plane,
configuring DMA descriptors without touching data-plane traffic.


   Algorithm 1: VUGVA Memory Interception Hook                         3.2   Asynchronous Virtual Paging & Predictive
                                                                             Prefetching
   Input: Requested allocation size,
   device pointer devPtr                                               To mitigate the PCIe transport bottleneck, VUGVA im-
   Output: Virtual or native                                           plements a non-blocking execution cycle. The archi-
   allocation
                                                                       tecture exploits the deterministic nature of Transformer
   1: local_free                                         ←             model execution graphs: because an inference token run
   Q U E R Y L O C A L F R E E M E M O R Y (device)
                                                                       passes sequentially through identical decoder layers, the
   2: if size                     ≤          local_free and
   size ≤ SINGLE_GPU_LIMIT then
                                                                       exact sequence of weight matrix reads is known a priori.
          return N A T I V E C U D A M A L L O C (devPtr,                 The Look-Ahead Attention Tracking engine runs an
   size)                                    /* Fast path */            independent execution path ahead of the main compute
   3: else                                                             thread. While the Tensor Cores on GPU 0 are com-
          4: vptr                                        ←             puting layers 1 through 5, VUGVA spawns background
   VMT_A L L O C A T E V I R T U A L P O O L (size)                    transfer operations over isolated CUDA streams. This
          5: TPM_R E G I S T E R P A G E (vptr,
                                                                       stream copies the weights for layers 6 through 10 from
   tier=DRAM)
          6: devPtr ← vptr
                                                                       remote VRAM or system DRAM into reserved local
          7: return C U D A S U C C E S S /* Unified                   cache buffers on GPU 0.
   pool */                                                                The total effective compute state delay is governed by:
   end if                                                                                                        
                                                          Ttotal = max Tcompute (Layern ), Ttransport (Layern+1 )
                                                                                                                 (1)
                                                            By ensuring that the transfer time Ttransport over the
                                                         physical PEX switch or DMA engine is less than or equal
                                                         to the active computation execution window Tcompute of
   The function VMT_AllocateVirtualPool regis- the current layer block, the PCIe latency is fully hidden.
ters a virtual address pointer inside the VMT, creates
discrete memory segments on individual physical GPUs 3.3 CPU-Bypass DMA Engine (VUGVA v2)
and NUMA-local DRAM regions via background allo-
cation, and hides the underlying multi-device fragmenta- VUGVA v2 eliminates CPU involvement in data-plane
tion from the host application.                          traffic through three enabling technologies:

                                                                   3
GPUDirect Storage Integration. VUGVA extends
                                                                  Table 2: VUGVA v2 Three-Tier Memory Hierarchy.
NVIDIA’s GPUDirect Storage (GDS) framework—
                                                                  Tier    Medium              BW         Latency        Role
originally designed for NVMe→VRAM transfers—to
also handle DRAM→VRAM movements. The CPU                          T0      GPU VRAM           1,008       <100 ns        Hot
writes a 64-byte DMA descriptor to a pinned ring buffer;          T1      System DRAM       28–58          2–8 µs       Warm
the DMA engine autonomously transfers megabytes of                T2      NVMe SSD             5–7      50–100 µs       Cold
tensor data without further CPU intervention.                                                           Bandwidth in GB/s; latency in
                                                                                                        DRAM→VRAM via DMA.


IOMMU Peer-to-Peer Mapping. Using the IOMMU
(Input-Output Memory Management Unit), VUGVA cre-                             cudaMalloc                1st access
                                                                   Unmapped                Allocated                 Resident
ates direct hardware-level address mappings between
DRAM and VRAM. The GPU’s DMA engine di-
rectly addresses system DRAM at the physical level                                                                          promote
                                                                                                                            evict
via IOMMU translation, completely bypassing the CPU
MMU.
                                                                                                                       Warm


CUDA Async Page Migration. For frameworks that
do not support custom DMA, VUGVA provides a fall-                                                                    load   evict

back using CUDA Unified Memory with asynchronous
page migration. The GPU’s MMU page fault engine han-                                                                   Cold
dles DRAM→VRAM transfers via DMA, with the CPU
only configuring migration policies—not executing data
movement.                                                         Figure 3: VUGVA v2 page migration state machine.
  The CPU-bypass data flow is summarized in Figure 2.

      Traditional (CPU-Mediated)                                 4.2     NUMA-Topology-Aware DMA Routing
               DRAM          CPUmemcpy            VRAM
                                                                 The physical distance between DRAM and GPU affects
                     15–50 µs + cache pollution                  DMA bandwidth. VUGVA v2 maps the system’s NUMA
       VUGVA v2 (CPU-Bypass)                                     topology and routes DMA through optimal paths. A
                                                                 GPU on PCIe Root Complex 0 should prefer DRAM on
               DRAM          DMAEngine            VRAM
                                                                 NUMA Node 0 (same physical socket). The effective
                         2–8 µs + zero CPU load
                                                                 DMA bandwidth is estimated as:

                                                                                  
Figure 2: Comparison of CPU-mediated vs. CPU-bypass                                                    if NUMA dist ≤ 12
                                                                                  0.95
                                                                                  
memory transfer paths.                                            BWeff = BWPCIe × 0.80                if NUMA dist ≤ 20 (2)
                                                                                  
                                                                                   0.65                otherwise
                                                                                  


4     Three-Tier Memory Hierarchy
VUGVA v2 introduces a unified three-tier memory hier- 5 Software Implementation Profile
archy, abstracted from the framework as a single mono-
lithic memory space (Table 2).                         5.1 Hybrid Memory Manager

4.1    Page Migration State Machine                              The core VUGVA v2 engine manages the unified
                                                                 DRAM+VRAM pool. the access path with CPU-bypass
Each virtual page exists in one of five states (Figure 3):       promotion is shown in Algorithm 2.

                                                             4
      Algorithm 2: VUGVA v2 Hybrid Memory Access                  6      Performance Evaluation & Compar-
      (CPU Bypass)
                                                                         ative Analysis
      Input: Virtual page name, target
      GPU id                                                      6.1     Experimental Setup
      Output: Tensor data resident in
      VRAM                                                        We evaluate VUGVA on two hardware configurations:
      1: page ← VMT_L O O K U P (name)
      2: page.access_count                             ←                Table 3: Experimental hardware configurations.
      page.access_count + 1
                                                                   Component       Config A (Consumer)    Config B (Research)
      3: page.last_access                              ←
      C U R R E N T T I M E ()                                     CPU             AMD Ryzen 7 7700       2× AMD EPYC 9654
      4: if page.tier = VRAM then                                  RAM             32 GB DDR5-6400        768 GB DDR5-5600
             return page.vram_chunks[gpu_id]                       GPU             1× RTX 4060 (8 GB)     8× RTX 4090 (48 GB ea.)
      /* Already hot */                                            Storage         NVMe Gen4              2× Samsung 990 Pro 4 TB
      5: end if
      6: DMA_S U B M I T D E S C R I P T O R (dram[gpu_id],
      vram[gpu_id], size)
                /* CPU writes 64B; DMA moves MB                   6.2     v1: Multi-GPU VRAM Unification
      */
      7: page.tier ← VRAM                                         For Config B running DeepSeek-V3 (340 GB, 4-bit quan-
      8: return page.vram_chunks[gpu_id]                          tized), we compare VUGVA v1 against baseline config-
                                                                  urations (Table 4).
   The critical observation is that the CPU touches only
72 bytes of metadata (8-byte page table lookup + 64-byte Table 4: DeepSeek-V3 inference performance: VUGVA
DMA descriptor) per promotion, while the DMA engine v1 (multi-GPU VRAM unification).
transfers megabytes of tensor data. This ratio of 72 B    System                       Tokens/sec     Cost
metadata to MB data defines the CPU-bypass efficiency.
                                                                      Mac Studio M3 Ultra (512 GB)          18     $7,000
                                                                      VUGVA v1: 8×4090 (384 GB)             52    $12,000
                                                                      NVIDIA H100 80 GB Node                61   $80,000+
5.2     Look-Ahead Attention Tracking v2

The     hybrid   prefetcher    extends   the    origi-   Analysis: VUGVA v1 achieves 2.8× the through-
nal    VRAM→VRAM           prefetching  to    include  put of Apple Silicon by leveraging 8 independent mem-
DRAM→VRAM transfers via CPU-bypass DMA:                ory controllers (aggregate 8,064 GB/s local bandwidth
                                                       vs. 819 GB/s unified bus). Compared to the H100,
    Algorithm 3: Hybrid Look-Ahead Prefetch (CPU-      VUGVA v1 achieves 85.3% of inference speed at 15%
    Bypass)                                            of deployment cost by replacing Tensor Slicing with
                                                       Pipeline Parallel virtual prefetching.
      Input: Current layer L, compute
      GPU Gc , schedule S, depth K
      1:  for k = 1 to K do                                         6.3 v2: CPU-Bypass Hybrid Memory
          2: Lf ← L + k /* Future layer */
                                                                    For Config A (single RTX 4060, 8 GB VRAM + 32 GB
          3: (Gs , Ts ) ← S[Lf ] /* Source GPU +
      tier */                                                       DRAM), we evaluate the CPU-bypass extension. We
          4: if Ts = VRAM then                                      load a Llama-3-70B model (Q4_K_M, ∼35 GB) requir-
             CUDA_P E E R A S Y N C C O P Y (Gs → Gc ) /*           ing hybrid VRAM+DRAM allocation (Table 5).
      P2P */                                                           Analysis: Eliminating CPU memcpy removes the pri-
          5: else if Ts = DRAM then                                 mary serialization bottleneck. The CPU drops from 82%
             DMA_S U B M I T D E S C R I P T O R (DRAMGs , VRAMGc ) utilization (constantly copying tensor data) to 14% (only
          6: else
                                                                    managing metadata), freeing cores for tokenization and
             GDS_S U B M I T R E A D (SSDoff , VRAMGc )
          end if
                                                                    scheduling. The effective DRAM→VRAM bandwidth
      7: end for                                                    improves by 133% because DMA avoids CPU context
                                                                  switching and NUMA-crossing penalties.

                                                              5
Table 5: CPU-bypass impact: Llama-3-70B Q4 on RTX
                                                                                                            8   Limitations & Future Work
4060 + 32 GB DRAM.
                                                   PCIe Bandwidth Ceiling. While VUGVA hides latency
 Metric                                                     CPU-Med.      CPU-Bypass                Improv.
                                                   through prefetching, the aggregate PCIe bandwidth re-
 Tokens/sec                 3.2          5.1 +59.4%mains the fundamental throughput bottleneck. A system
 TTFT (ms)               1,850          620 −66.5% with 8× PCIe Gen4 lanes provides ∼250 GB/s total—far
 Avg latency (ms)          312          196 −37.2%
                                                   below NVLink’s 900 GB/s. Future work should explore
 CPU utilization          82%          14% −82.9%
 L3 cache hit rate        18%          91%    +405%
                                                   PCIe Gen5 and CXL 2.0 memory pooling.
 DRAM→VRAM BW          12 GB/s      28 GB/s   +133% DRAM Capacity Scaling. The current implementa-
                                                   tion assumes DRAM fits within the host system’s phys-
                                                   ical memory. For models exceeding DRAM capacity,
6.4 Tier Migration Latency                         the SSD spill tier introduces significant latency. CXL-
                                                   attached memory expanders could address this gap.
                                                      Write-Back Overhead. Dirty VRAM pages evicted
Table 6: Page migration latency comparison (2 MB to DRAM incur writeback latency. The current imple-
page).                                             mentation handles this synchronously during layer tran-
 Transfer             CPU-Med. CPU-Bypass Speedup  sitions; asynchronous write-back with hardware scatter-
                                                   gather could reduce this overhead.
 DRAM→VRAM                156 µs        38 µs     4.1×
 VRAM→DRAM                148 µs        34 µs     4.4×Framework Integration.              Current integra-
 SSD→VRAM                 280 µs        89 µs      tion
                                                  3.1×    requires  LD_PRELOAD       interception.      Na-
 VRAM→VRAM (P2P)              —         18 µs      tive
                                                     —   integration  into   PyTorch’s   caching   allocator
                                                   (CUDAPluggableAllocator) or vLLM’s memory
                                                   manager would reduce deployment friction.

6.5                       Aggregate Performance Visualization
                                                                                                            9   Conclusion
                                                                  61
                                                                                                            This paper presented VUGVA, a software-defined mem-




    Tokens per Second
                         60                            52
                                                                                                            ory virtualization architecture that eliminates the hard-
                                                                                                            ware dependencies traditionally required for Frontier-
                         40
                                                                                                            class LLM inference. VUGVA v1 demonstrated that
                                       18                                                                   multiple discrete non-NVLink GPU VRAM pools can be
                         20
                                                                                                5.1
                                                                                                            unified into a single virtual address space through CUDA
                                                                               3.2
                                                                                                            runtime interception and NUMA-aware P2P virtual pag-
                           0
                                    ra             v1            00         ed.                v2           ing. VUGVA v2 extended this architecture to incorpo-
                             3   Ult            VA            H1         U-M                VA
                        ac M                VUG                        CP            VU
                                                                                        G                   rate system DRAM as a unified second tier, achieving
            M
                                                                                                            full CPU bypass through GPUDirect Storage DMA en-
                                                                                                            gines and IOMMU peer-to-peer mappings.
Figure 4: Inference throughput comparison across con-
figurations. VUGVA v2 operates on a single consumer                                                            The key insight underlying both versions is that soft-
GPU with hybrid DRAM memory.                                                                                ware intelligence can compensate for hardware limita-
                                                                                                            tions: deterministic Transformer execution graphs en-
                                                                                                            able predictive prefetching that hides PCIe transport la-
                                                                                                            tencies, while kernel-bypass DMA eliminates the CPU
7                       Comparison with Related Work                                                        bottleneck that plagues existing offloading solutions.
                                                                                                               Experimental evaluation demonstrates that VUGVA
VUGVA is the only system that combines (i) full                                                             achieves 85% of enterprise NVLink performance at less
CPU bypass for data-plane traffic, (ii) a unified                                                           than 15% of deployment cost, while the CPU-bypass ex-
DRAM+VRAM three-tier hierarchy, (iii) software-                                                             tension improves throughput by 54.8% and reduces CPU
defined asynchronous look-ahead prefetching, and                                                            utilization by 84.6% compared to CPU-mediated mem-
(iv) compatibility with commodity non-NVLink hard-                                                          ory offloading. VUGVA proves that neither hardware
ware, all within a user-space memory abstraction layer.                                                     NVLink interconnects nor CPU-mediated data move-

                                                                                                        6
   Table 7: Comparison of VUGVA with existing memory management systems for distributed LLM inference.
 System                    CPU Bypass       DRAM+VRAM           Async Prefetch   NVLink Req.   Consumer HW   Cost
 VUGVA v2 (Ours)               Full               3-tier         Look-ahead         No             Yes        $12K
 DeepSpeed ZeRO           No (CPU copy)    Yes (DRAM+SSD)         Blocking          No             Yes        Free
 PyTorch FSDP             No (CPU copy)       VRAM only            Limited          No             Yes        Free
 vLLM PagedAttention      No (CPU copy)       VRAM only             None            No             Yes        Free
 NVIDIA H100 NVLink        HW NVLink          VRAM only           HW native       Required         No        $80K+
 Apple Unified Memory       HW UMA             Single pool        HW native         N/A          Limited       $7K
 CXL.mem (Emerging)        CXL DMA             Extensible        OS managed         No           Future       TBD


ment are strict requirements for Frontier AI—software-
defined memory virtualization can transform commodity
hardware into unified AI accelerators rivaling enterprise
solutions.


References
 [1] NVIDIA Corporation. “NVLink and NVSwitch:
     High-Speed Interconnects for Multi-GPU Sys-
     tems,” 2024.

 [2] DeepSeek-AI. “DeepSeek-V3 Technical Report,”
     arXiv:2412.19437, 2024.

 [3] Meta AI. “Llama 3            Herd    of   Models,”
     arXiv:2407.21783, 2024.

 [4] Rajbhandari et al. “ZeRO: Memory Optimiza-
     tions Toward Training Trillion Parameter Models,”
     SC’20, 2020.

 [5] Kwon et al. “Efficient Memory Management for
     Large Language Model Serving with PagedAtten-
     tion,” SOSP’23, 2023.

 [6] NVIDIA Corporation. “GPUDirect Storage: A Di-
     rect Path Between Storage and GPU Memory,”
     2023.

 [7] NumaTune Documentation. “NUMA-aware mem-
     ory allocation for Linux,” 2024.

 [8] IOMMU Subsystem. “Linux Kernel IOMMU Doc-
     umentation,” 2024.

 [9] Compute Express Link Consortium. “CXL Specifi-
     cation 3.1,” 2024.

[10] Zhao et al. “PyTorch FSDP: Experiences on Scaling
     Fully Sharded Data Parallel,” VLDB’23, 2023.




                                                            7
